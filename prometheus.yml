# Prometheus scrape config for the Store Intelligence API.
# Used by the optional `prometheus` service in docker-compose.yml.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: store-intel-api
    metrics_path: /internal/metrics
    static_configs:
      - targets: ["api:8000"]
